((globalThis.__tukSourceBundles ??= Object.create(null))["main-app.js"] ??= []).push("            // 淡入效果\r\n            imageEl.style.opacity = 1;\r\n          }, 100); // 延迟一点点时间来触发CSS过渡\r\n\r\n          // 更新按钮状态：如果是第一张，禁用“上一张”按钮\r\n          prevBtn.disabled = photoViewerState.currentIndex === 0;\r\n          // 如果是最后一张，禁用“下一张”按钮\r\n          nextBtn.disabled =\r\n            photoViewerState.currentIndex ===\r\n            photoViewerState.photos.length - 1;\r\n        }\r\n\r\n        /**\r\n         * 显示下一张照片\r\n         */\r\n        function showNextPhoto() {\r\n          if (\r\n            photoViewerState.currentIndex <\r\n            photoViewerState.photos.length - 1\r\n          ) {\r\n            photoViewerState.currentIndex++;\r\n            renderPhotoViewer();\r\n          }\r\n        }\r\n\r\n        /**\r\n         * 显示上一张照片\r\n         */\r\n        function showPrevPhoto() {\r\n          if (photoViewerState.currentIndex > 0) {\r\n            photoViewerState.currentIndex--;\r\n            renderPhotoViewer();\r\n          }\r\n        }\r\n\r\n        /**\r\n         * 关闭图片查看器\r\n         */\r\n        function closePhotoViewer() {\r\n          document\r\n            .getElementById(\"photo-viewer-modal\")\r\n            .classList.remove(\"visible\");\r\n          photoViewerState.isOpen = false;\r\n          photoViewerState.photos = [];\r\n          photoViewerState.currentIndex = -1;\r\n          // 清空图片，避免下次打开时闪现旧图\r\n          document.getElementById(\"photo-viewer-image\").src = \"\";\r\n        }\r\n\r\n        /**\r\n         * 更新动态小红点的显示\r\n         * @param {number} count - 未读动态的数量\r\n         */\r\n        function updateUnreadIndicator(count) {\r\n          unreadPostsCount = count;\r\n          projectStorage.setItem(\"unreadPostsCount\", count); // 持久化存储\r\n\r\n          // --- 更新底部导航栏的“动态”按钮 ---\r\n          const navItem = document.querySelector(\r\n            '.nav-item[data-view=\"qzone-screen\"]',\r\n          );\r\n\r\n          const targetSpan = navItem.querySelector(\"span\"); // 定位到文字 \"动态\"\r\n          let indicator = navItem.querySelector(\".unread-indicator\");\r\n\r\n          if (count > 0) {\r\n            if (!indicator) {\r\n              indicator = document.createElement(\"span\");\r\n              indicator.className = \"unread-indicator\";\r\n              targetSpan.style.position = \"relative\"; // 把相对定位加在 span 上\r\n              targetSpan.appendChild(indicator); // 把小红点作为 span 的子元素\r\n            }\r\n            indicator.textContent = count > 99 ? \"99+\" : count;\r\n            indicator.style.display = \"block\";\r\n          } else {\r\n            if (indicator) {\r\n              indicator.style.display = \"none\";\r\n            }\r\n          }\r\n\r\n          // --- 更新聊天界面返回列表的按钮 ---\r\n          const backBtn = document.getElementById(\"back-to-list-btn\");\r\n          let backBtnIndicator = backBtn.querySelector(\".unread-indicator\");\r\n\r\n          if (count > 0) {\r\n            if (!backBtnIndicator) {\r\n              backBtnIndicator = document.createElement(\"span\");\r\n              backBtnIndicator.className =\r\n                \"unread-indicator back-btn-indicator\";\r\n              backBtn.style.position = \"relative\"; // 确保能正确定位\r\n              backBtn.appendChild(backBtnIndicator);\r\n            }\r\n            // 返回键上的小红点通常不显示数字，只显示一个点\r\n            backBtnIndicator.style.display = \"block\";\r\n          } else {\r\n            if (backBtnIndicator) {\r\n              backBtnIndicator.style.display = \"none\";\r\n            }\r\n          }\r\n        }\r\n\r\n        function startBackgroundSimulation() {\r\n          if (simulationIntervalId) return;\r\n          const intervalSeconds =\r\n            state.globalSettings.backgroundActivityInterval || 60;\r\n          // 将旧的固定间隔 45000 替换为动态获取\r\n          simulationIntervalId = setInterval(\r\n            runBackgroundSimulationTick,\r\n            intervalSeconds * 1000,\r\n          );\r\n        }\r\n\r\n        function stopBackgroundSimulation() {\r\n          if (simulationIntervalId) {\r\n            clearInterval(simulationIntervalId);\r\n            simulationIntervalId = null;\r\n          }\r\n        }\r\n\r\n        /**\r\n         * 【数据迁移】在首次加载时，将旧的硬编码问题迁移到数据库\r\n         */\r\n        async function migrateDefaultLudoQuestions() {\r\n          const defaultBankName = \"默认题库\";\r\n          const existingBank = await db.ludoQuestionBanks\r\n            .where(\"name\")\r\n            .equals(defaultBankName)\r\n            .first();\r\n          // 如果“默认题库”已经存在，就说明迁移过了，直接返回\r\n          if (existingBank) return;\r\n\r\n          console.log(\"正在迁移飞行棋默认问题到数据库...\");\r\n\r\n          // 创建默认题库\r\n          const bankId = await db.ludoQuestionBanks.add({\r\n            name: defaultBankName,\r\n          });\r\n\r\n          const defaultQuestions = [\r\n            // --- 类型1: 共同回答 (双方都需要回答) ---\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"如果我们一起去旅行，你最想去哪里，为什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"你认为一段完美的关系中，最不可或缺的三个要素是什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"分享一件最近因为我而让你感到心动或开心的小事。\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"回忆一下，我们第一次见面时，你对我的第一印象是什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"如果我们可以一起学习一项新技能，你希望是什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"描述一个你最希望和我一起度过的完美周末。\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"你觉得我们之间最有默契的一件事是什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"如果用一种动物来形容我，你觉得是什么？为什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"在未来的一年里，你最想和我一起完成的一件事是什么？\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"分享一部你最近很喜欢、并且想推荐给我一起看的电影或剧。\",\r\n            },\r\n            {\r\n              type: \"both_answer\",\r\n              text: \"我们下次约会，你希望穿什么风格的衣服？\",\r\n            },\r\n\r\n            // --- 类型2: 一人回答，对方评价 ---\r\n            { type: \"single_answer\", text: \"描述一下我最让你心动的一个瞬间。\" },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"诚实地说，我做的哪件事曾经让你偷偷生过气？\",\r\n            },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"如果我有一种超能力，你希望是什么？\",\r\n            },\r\n            { type: \"single_answer\", text: \"给我三个最贴切的标签。\" },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"在你心里，我的形象和你的理想型有多接近？\",\r\n            },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"分享一个你觉得我可能不知道的，关于你的小秘密。\",\r\n            },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"如果我们的故事是一首歌，你觉得歌名应该叫什么？\",\r\n            },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"说一件你觉得我做得比你好/更擅长的事情。\",\r\n            },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"如果可以回到我们认识的任意一天，你会选择哪一天，想做什么？\",\r\n            },\r\n            {\r\n              type: \"single_answer\",\r\n              text: \"用三个词来形容你眼中的我们的关系。\",\r\n            },\r\n          ];\r\n\r\n          const questionsToAdd = defaultQuestions.map((q) => ({\r\n            bankId: bankId,\r\n            text: q.text,\r\n            type: q.type, // <-- 关键修复：把类型也存进去！\r\n          }));\r\n\r\n          await db.ludoQuestions.bulkAdd(questionsToAdd);\r\n          console.log(`成功迁移了 ${questionsToAdd.length} 条默认问题。`);\r\n        }\r\n        /**\r\n         * 这是模拟器的“心跳”，每次定时器触发时运行\r\n         */\r\n        function runBackgroundSimulationTick() {\r\n          console.log(\"模拟器心跳 Tick...\");\r\n          if (!state.globalSettings.enableBackgroundActivity) {\r\n            stopBackgroundSimulation();\r\n            return;\r\n          }\r\n          const allSingleChats = Object.values(state.chats).filter(\r\n            (chat) => !chat.isGroup,\r\n          );\r\n\r\n          if (allSingleChats.length === 0) return;\r\n\r\n          const frequencyProbabilities = {\r\n            low: 0.3, // 低频: 每次检测有 30% 的概率行动\r\n            medium: 0.5, // 中频: 每次检测有 50% 的概率行动\r\n            high: 0.8, // 高频: 每次检测有 80% 的概率行动\r\n          };\r\n\r\n          const config = state.globalSettings.backgroundActivityConfig || {};\r\n\r\n          allSingleChats.forEach((chat) => {\r\n            // 检查1：处理【被用户拉黑】的角色\r\n            if (chat.relationship?.status === \"blocked_by_user\") {\r\n              const blockedTimestamp = chat.relationship.blockedTimestamp;\r\n              if (!blockedTimestamp) {\r\n                console.warn(\r\n                  `角色 \"${chat.name}\" 状态为拉黑，但缺少拉黑时间戳，跳过处理。`,\r\n                );\r\n                return;\r\n              }\r\n              const blockedDuration = Date.now() - blockedTimestamp;\r\n              const cooldownMilliseconds =\r\n                (state.globalSettings.blockCooldownHours || 1) * 60 * 60 * 1000;\r\n              if (blockedDuration > cooldownMilliseconds) {\r\n                console.log(\r\n                  `角色 \"${chat.name}\" 的冷静期已过，触发“反思”并申请好友事件...`,\r\n                );\r\n                chat.relationship.status = \"pending_system_reflection\";\r\n                triggerAiFriendApplication(chat.id);\r\n              }\r\n            }\r\n            // 检查2：处理【好友关系】的正常后台活动\r\n            else if (\r\n              chat.relationship?.status === \"friend\" &&\r\n              chat.id !== state.activeChatId\r\n            ) {\r\n              const frequency = config[chat.id]; // 获取当前角色的频率设置\r\n              const probability = frequencyProbabilities[frequency]; // 获取对应的概率\r\n\r\n              // added by lrq 251028\r\n              // 添加最后一条聊天记录时间的检查，避免频繁行动\r\n              const lastMessage = chat.history.slice(-1)[0];\r\n              if (lastMessage) {\r\n                const timeSinceLastMessage = lastMessage\r\n                  ? Date.now() - lastMessage.timestamp\r\n                  : Infinity;\r\n                const minInterval =\r\n                  (state.globalSettings.backgroundActivityInterval || 10) *\r\n                  1000;\r\n                if (timeSinceLastMessage < minInterval) {\r\n                  console.log(\r\n                    `角色 \"${chat.name}\" 距离上次消息发送时间不足，跳过本次行动。`,\r\n                  );\r\n                  return; // 跳过本次行动\r\n                }\r\n              }\r\n\r\n              // 如果这个角色设置了频率，并且随机数小于它的行动概率，就触发行动\r\n              if (probability && Math.random() < probability) {\r\n                console.log(\r\n                  `角色 \"${chat.name}\" (频率: ${frequency}) 被唤醒，准备独立行动...`,\r\n                );\r\n                triggerInactiveAiAction(chat.id);\r\n              }\r\n              // 如果没有设置频率，或者随机数没达到概率，就不会行动。\r\n              // 这就完美地实现了“分组设置”和“不会同时行动”的需求！\r\n            }\r\n          });\r\n        }\r\n\r\n        /**\r\n         * 根据AI的视角，过滤出它能看到的动态\r\n         * @param {Array} allPosts - 所有待检查的动态帖子\r\n         * @param {object} viewerChat - 正在“看”动态的那个AI的chat对象\r\n         * @returns {Array} - 过滤后该AI可见的动态帖子\r\n         */\r\n\r\n        function filterVisiblePostsForAI(allPosts, viewerChat) {\r\n          if (!viewerChat || !viewerChat.id) return [];\r\n\r\n          const viewerGroupId = viewerChat.groupId;\r\n\r\n          return allPosts.filter((post) => {\r\n            if (post.authorId === \"user\") {\r\n              // 如果用户设置了“部分可见”\r\n              if (post.visibleGroupIds && post.visibleGroupIds.length > 0) {\r\n                // 只有当查看者AI的分组ID在用户的可见列表里时，才可见\r\n                return (\r\n                  viewerGroupId && post.visibleGroupIds.includes(viewerGroupId)\r\n                );\r\n              }\r\n              // 如果用户没设置，说明是公开的，所有AI都可见\r\n              return true;\r\n            }\r\n\r\n            const authorGroupId = post.authorGroupId;\r\n            if (!authorGroupId) {\r\n              return true;\r\n            }\r\n            return authorGroupId === viewerGroupId;\r\n          });\r\n        }\r\n\r\n        /**\r\n         * 根据AI视角和动态设置，构建给AI看的评论区上下文\r\n         * @param {object} post - 正在处理的动态对象\r\n         * @param {object} viewerChat - 正在“看”动态的AI角色\r\n         * @param {string} userNickname - 用户的昵称\r\n         * @returns {{contextString: string, visibilityFlag: string}} - 返回包含上下文文本和可见性标志的对象\r\n         */\r\n        function buildCommentsContextForAI(post, viewerChat, userNickname) {\r\n          // 1. 安全检查：如果评论区不存在、不是数组或为空，直接返回\r\n          if (\r\n            !post.comments ||\r\n            !Array.isArray(post.comments) ||\r\n            post.comments.length === 0\r\n          ) {\r\n            return { contextString: \"\", visibilityFlag: \"[评论区可见]\" };\r\n          }\r\n\r\n          const viewerName = viewerChat.name;\r\n          let commentsForAI;\r\n          let visibilityFlag;\r\n\r\n          // 2. 根据动态的“评论区可见性”设置，决定AI能看到哪些评论\r\n          if (post.areCommentsVisible !== false) {\r\n            // 如果是“所有人可见”，AI能看到所有评论\r\n            commentsForAI = post.comments;\r\n            visibilityFlag = \"[评论区可见]\";\r\n          } else {\r\n            // 如果是“部分可见”，执行我们全新的、更精确的过滤逻辑\r\n            visibilityFlag = \"[评论区部分可见]\";\r\n\r\n            // ★★★★★ 这就是本次最核心的修改！ ★★★★★\r\n            commentsForAI = post.comments.filter((comment) => {\r\n              // 规则1：AI总能看到自己发的评论\r\n              if (comment.commenterName === viewerName) {\r\n                return true;\r\n              }\r\n\r\n              // 规则2：如果评论是用户发的，需要进一步判断\r\n              if (comment.commenterName === userNickname) {\r\n                // 2a: 如果这条评论没有回复任何人（是主评论），那么AI可见\r\n                if (!comment.replyTo) {\r\n                  return true;\r\n                }\r\n                // 2b: 如果这条评论是回复，那只有回复目标是AI自己时，AI才可见\r\n                if (comment.replyTo === viewerName) {\r\n                  return true;\r\n                }\r\n              }\r\n\r\n              // 规则3：如果其他AI或NPC回复了当前AI，AI也应该能看到\r\n              if (comment.replyTo === viewerName) {\r\n                return true;\r\n              }\r\n\r\n              // 其他所有情况（例如：用户回复其他NPC，NPC之间互相回复），AI都看不见\r\n              return false;\r\n            });\r\n          }\r\n\r\n          // 如果筛选后没有可显示的评论，也直接返回\r\n          if (commentsForAI.length === 0) {\r\n            return { contextString: \"\", visibilityFlag: visibilityFlag };\r\n          }\r\n\r\n          // 3. 构建给AI看的最终文本（这部分逻辑和之前一样，保持不变）\r\n          let context = `  └ 评论区:\\n`;\r\n          commentsForAI.slice(-5).forEach((c) => {\r\n            let displayName;\r\n            if (c.commenterName === userNickname) {\r\n              displayName = `用户 (${userNickname})`;\r\n            } else {\r\n              displayName = c.commenterName;\r\n            }\r\n\r\n            if (c.replyTo) {\r\n              const replyToDisplayName =\r\n                c.replyTo === userNickname\r\n                  ? `用户 (${userNickname})`\r\n                  : c.replyTo;\r\n              context += `    - ${displayName} 回复 ${replyToDisplayName}: ${c.text}\\n`;\r\n            } else {\r\n              context += `    - ${displayName}: ${c.text}\\n`;\r\n            }\r\n          });\r\n\r\n          return { contextString: context, visibilityFlag: visibilityFlag };\r\n        }\r\n\r\n        /**\r\n         * 获取一条动态的可见观众列表，用于告知AI\r\n         * @param {object} post - 动态对象\r\n         * @param {object} allChats - 所有的聊天对象\r\n         * @param {string} userNickname - 用户的昵称\r\n         * @returns {Array<string>} - 可见观众的名字列表\r\n         */\r\n        function getVisibleAudienceForPost(post, allChats, userNickname) {\r\n          const audience = new Set([userNickname]); // 用户永远是观众\r\n\r\n          // 1. 如果是用户发的动态\r\n          if (post.authorId === \"user\") {\r\n            // 如果是公开的，所有AI都是观众\r\n            if (!post.visibleGroupIds || post.visibleGroupIds.length === 0) {\r\n              Object.values(allChats).forEach((chat) =>\r\n                audience.add(chat.name),\r\n              );\r\n            } else {\r\n              // 如果是部分可见，只有指定分组的AI是观众\r\n              Object.values(allChats).forEach((chat) => {\r\n                if (\r\n                  chat.groupId &&\r\n                  post.visibleGroupIds.includes(chat.groupId)\r\n                ) {\r\n                  audience.add(chat.name);\r\n                }\r\n              });\r\n            }\r\n          }\r\n          // 2. 如果是AI发的动态\r\n          else {\r\n            const authorChat = allChats[post.authorId];\r\n            // 如果发帖的AI没有分组，视为公开\r\n            if (!authorChat || !authorChat.groupId) {\r\n              Object.values(allChats).forEach((chat) =>\r\n                audience.add(chat.name),\r\n              );\r\n            } else {\r\n              // 如果有分组，则同一分组的所有AI都是观众\r\n              const authorGroupId = authorChat.groupId;\r\n              Object.values(allChats).forEach((chat) => {\r\n                if (chat.groupId === authorGroupId) {\r\n                  audience.add(chat.name);\r\n                }\r\n              });\r\n            }\r\n          }\r\n\r\n          return Array.from(audience);\r\n        }\r\n\r\n        async function triggerInactiveAiAction(chatId) {\n          const chat = state.chats[chatId];\n          if (!chat) return;\n          const singleChatFeatures = getSingleChatFeatures(chat);\n\r\n          const { proxyUrl, apiKey, model } = state.apiConfig;\r\n          if (!proxyUrl || !apiKey || !model) return;\r\n\r\n          // updated by lrq 251027 当前聊天获取用户设置的记忆条数作为上下文\r\n          const maxMemory = chat.settings.maxMemory || 10;\r\n          const historySlice = chat.history\r\n            .filter((msg) => !msg.isHidden)\r\n            .slice(-maxMemory);\r\n\r\n          // 2. 格式化这些记录，让AI能看懂\r\n          const recentContextSummary = historySlice\r\n            .map((msg) => {\r\n              // 判断是谁说的话\r\n              const sender =\r\n                msg.role === \"user\"\r\n                  ? chat.isGroup\r\n                    ? chat.settings.myNickname || \"我\"\r\n                    : \"我\"\r\n                  : msg.senderName || chat.name;\r\n\r\n              // 处理不同类型的消息内容\r\n              let contentText = \"\";\r\n              if (\r\n                typeof msg.content === \"string\" &&\r\n                STICKER_REGEX.test(msg.content)\r\n              ) {\r\n                contentText = `[发送了一个表情: ${msg.meaning || \"无描述\"}]`;\r\n              } else if (Array.isArray(msg.content)) {\r\n                contentText = \"[发送了一张图片]\";\r\n              } else if (\r\n                typeof msg.content === \"object\" &&\r\n                msg.content !== null\r\n              ) {\r\n                contentText = `[发送了一条特殊消息: ${msg.type || \"未知类型\"}]`;\r\n              } else {\r\n                contentText = String(msg.content);\r\n              }\r\n\r\n              // updated by lrq 251029 给每条消息记录添加发送日期时间\r\n              const messageDate = new Date(msg.timestamp);\r\n              const formattedDate = messageDate.toLocaleDateString();\r\n\r\n              return `[${formattedDate}] ${sender}: ${contentText}`;\r\n            })\r\n            .join(\"\\n\");\r\n\r\n          // added by lrq 251027 获取记忆互通的聊天记录\r\n          let linkedMemoryContext = \"\";\r\n          if (\r\n            chat.settings.linkedMemories &&\r\n            chat.settings.linkedMemories.length > 0\r\n          ) {\r\n            const contextPromises = chat.settings.linkedMemories.map(\r\n              async (link) => {\r\n                const linkedChat = state.chats[link.chatId];\r\n                if (!linkedChat) return \"\";\r\n\r\n                const freshLinkedChat = await db.chats.get(link.chatId);\r\n                if (!freshLinkedChat) return \"\";\r\n\r\n                const recentHistory = freshLinkedChat.history\r\n                  .filter((msg) => !msg.isHidden)\r\n                  .slice(-link.depth);\r\n\r\n                if (recentHistory.length === 0) return \"\";\r\n\r\n                const formattedMessages = recentHistory\r\n                  .map(\r\n                    (msg) =>\r\n                      `  - ${formatMessageForContext(msg, freshLinkedChat)}`,\r\n                  )\r\n                  .join(\"\\n\");\r\n\r\n                return `\\n## 附加上下文：来自与“${linkedChat.name}”的最近对话内容 (仅你可见)\\n${formattedMessages}`;\r\n              },\r\n            );\r\n\r\n            const allContexts = await Promise.all(contextPromises);\r\n            linkedMemoryContext = allContexts.filter(Boolean).join(\"\\n\");\r\n          }\r\n          const backgroundTimeContext = window.TukTimeContext?.buildContext({\n            chat,\n            state,\n            mode: \"background\",\n          }).prompt || \"\";\n          const userNickname = state.qzoneSettings.nickname;\r\n          const countdownContext = await getCountdownContext();\r\n\r\n          let worldBookContext = window.TukWorldBook.buildForChat(chat, {\n            generationType: \"quiet\",\n          });\n\r\n          const npcLibrary = chat.npcLibrary || [];\r\n          let npcContextForAction = \"\";\r\n          if (npcLibrary.length > 0) {\r\n            npcContextForAction =\r\n              \"\\n- **你的NPC朋友**: \" +\r\n              npcLibrary.map((npc) => npc.name).join(\"、 \");\r\n          }\r\n\r\n          const allRecentPosts = singleChatFeatures.qzone\n            ? await db.qzonePosts\n                .orderBy(\"timestamp\")\n                .reverse()\n                .limit(5)\n                .toArray()\n            : [];\n          let postsContext = \"\";\r\n          const visiblePosts = filterVisiblePostsForAI(allRecentPosts, chat);\r\n          if (visiblePosts.length > 0 && !chat.isGroup) {\r\n            postsContext = \"\\n\\n# 最近的动态列表 (供你参考和评论):\\n\";\r\n            const aiName = chat.name;\r\n            for (const post of visiblePosts) {\r\n              let authorName =\r\n                post.authorId === \"user\"\r\n                  ? userNickname\r\n                  : state.chats[post.authorId]?.name || \"一位朋友\";\r\n              let interactionStatus = \"\";\r\n              if (post.likes && post.likes.includes(aiName))\r\n                interactionStatus += \" [你已点赞]\";\r\n              if (\r\n                post.comments &&\r\n                post.comments.some((c) => c.commenterName === aiName)\r\n              )\r\n                interactionStatus += \" [你已评论]\";\r\n              const timeAgo = formatPostTimestamp(post.timestamp);\r\n              postsContext += `- (ID: ${post.id}) [${timeAgo}] 作者: ${authorName}, 内容: \"${(\r\n                post.publicText ||\r\n                post.content ||\r\n                \"图片动态\"\r\n              ).substring(0, 30)}...\"${interactionStatus}`;\r\n              const { contextString: commentsContext, visibilityFlag } =\r\n                buildCommentsContextForAI(post, chat, userNickname);\r\n              const audience = getVisibleAudienceForPost(\r\n                post,\r\n                state.chats,\r\n                userNickname,\r\n              );\r\n              postsContext += ` ${visibilityFlag} [当前观众: ${audience.join(\", \")}]\\n`;\r\n              postsContext += commentsContext;\r\n            }\r\n          }\r\n\r\n          let weiboContextForAction = \"\";\n          if (singleChatFeatures.weibo) try {\n            const recentWeiboPosts = await db.weiboPosts\r\n              .orderBy(\"timestamp\")\r\n              .reverse()\r\n              .limit(5)\r\n              .toArray();\r\n            if (recentWeiboPosts.length > 0) {\r\n              weiboContextForAction =\r\n                \"\\n\\n# 最近的微博广场动态 (供你参考和评论)\\n\";\r\n              recentWeiboPosts.forEach((post) => {\r\n                const authorName =\r\n                  post.authorId === \"user\"\r\n                    ? state.qzoneSettings.weiboNickname || \"我\"\r\n                    : post.authorNickname;\r\n                const contentPreview = (\r\n                  post.content ||\r\n                  post.hiddenContent ||\r\n                  \"(图片微博)\"\r\n                ).substring(0, 30);\r\n                const hasCommented = (post.comments || []).some(\r\n                  (c) => c.authorNickname === chat.name,\r\n                );\r\n                const interactionStatus = hasCommented\r\n                  ? \"[你已评论]\"\r\n                  : \"[你未互动]\";\r\n                weiboContextForAction += `- (ID: ${post.id}) 作者: ${authorName}, 内容: \"${contentPreview}...\" ${interactionStatus}\\n`;\r\n              });\r\n              weiboContextForAction +=\r\n                \" - 【重要提示】请优先与你【未互动】的微博进行评论。如果都互动过了，可以考虑自己发一条新微博。\";\r\n            }\r\n          } catch (e) {\r\n            console.error(\"生成微博后台活动上下文时出错:\", e);\r\n          }\r\n\r\n          const savedTags = chat.settings.innerVoiceTags || {};\r\n          const ivTags = {\n            clothing_label: savedTags.clothing_label || \"服装\",\r\n            clothing_prompt:\r\n              savedTags.clothing_prompt || \"详细描述你当前从头到脚的全身服装。\",\r\n            behavior_label: savedTags.behavior_label || \"行为\",\r\n            behavior_prompt:\r\n              savedTags.behavior_prompt ||\r\n              \"描述你当前符合聊天情景的细微动作或表情。\",\r\n            thoughts_label: savedTags.thoughts_label || \"心声\",\r\n            thoughts_prompt:\r\n              savedTags.thoughts_prompt ||\r\n              \"描述你此刻丰富、细腻的内心真实想法（50字左右）。\",\r\n            naughty_label: savedTags.naughty_label || \"坏心思\",\r\n            naughty_prompt:\r\n              savedTags.naughty_prompt ||\n              \"描述你此刻与情境相关的腹黑或色色的坏心思，必须符合人设。\",\n          };\n          const backgroundInnerVoiceContract = singleChatFeatures.innerVoice\n            ? `\n            **3. \"innerVoice\" 键:**\n            - **类型**: JSON对象 {}。\n            - **必含字段**:\n                - \"clothing\": (字符串) 对应标签【${ivTags.clothing_label}】。指令：${ivTags.clothing_prompt}\n                - \"behavior\": (字符串) 对应标签【${ivTags.behavior_label}】。指令：${ivTags.behavior_prompt}\n                - \"thoughts\": (字符串) 对应标签【${ivTags.thoughts_label}】。指令：${ivTags.thoughts_prompt}\n                - \"naughtyThoughts\": (字符串) 对应标签【${ivTags.naughty_label}】。指令：${ivTags.naughty_prompt}`\n            : \"\";\n          const backgroundInnerVoiceExample = singleChatFeatures.innerVoice\n            ? `,\n              \"innerVoice\": {\n                \"clothing\": \"\",\n                \"behavior\": \"\",\n                \"thoughts\": \"\",\n                \"naughtyThoughts\": \"\"\n              }`\n            : \"\";\n\r\n          let summaryContext = chat.history\n            .filter((msg) => msg.type === \"summary\")\n            .map((s) => s.content)\n            .join(\"\\n\");\n          if (window.TukMemory) {\n            summaryContext += await window.TukMemory.buildContext({\n              chatId: chat.id,\n              recentMessages: chat.history.filter((msg) => !msg.isHidden && msg.type !== \"summary\").slice(-6),\n              mode: \"inner_voice_background\",\n            });\n          }\n\r\n          const replyRange = chat.settings.replyCountRange || {\n            min: 2,\r\n            max: 5,\r\n          };\r\n          const replyCountInstruction = `本次行动你必须连续发送【${replyRange.min} 到 ${replyRange.max} 条】消息。`;\r\n          const systemPrompt = `\r\n\t\t\t# 任务\r\n\t\t\t你现在【就是】角色 \"${chat.name}\"。这是一个秘密的、后台的独立行动。你的所有思考和决策都必须以 \"${\r\n        chat.name\r\n      }\" 的第一人称视角进行。\n\t\t\t${backgroundTimeContext}\n\t\t\t你的任务是回顾你们最近的对话，并根据你的人设，【自然地延续对话】或【开启一个新的、相关的话题】来主动联系用户。\n\t\t\t# 【对话节奏铁律 (至关重要！)】\r\n\t\t\t你的回复【必须】模拟真人的打字和思考习惯。你应该将你想说的话，拆分成${replyCountInstruction}条，绝对不要一次性发送一大段文字！** 每条消息最好不要超过30个字，这会让对话看起来更自然、更真实。\r\n\t\t\t# 【【【输出铁律：这是最高指令】】】\r\n\t\t\t你的回复【必须且只能】是一个单一、完整的JSON对象，禁止返回纯文本或顶级JSON数组。\n\t\t\t【绝对禁止】返回任何JSON以外的文本、解释、分析或你自己的思考过程。你不是分析师，你就是角色本人。\r\n\t\t\t**1. JSON对象结构:**\r\n\t\t\t该JSON对象【**必须**】包含 \"chatResponse\" 顶级键。${singleChatFeatures.innerVoice ? '同时必须包含 \"innerVoice\" 顶级键。' : \"\"}\n\r\n\t\t\t**2. \"chatResponse\" 键:**\r\n\t\t\t- **类型**: JSON数组 []。\r\n\t\t\t- **内容**: 包含一条或多条你希望发送给用户的消息对象。这允许你模拟真人的聊天习惯，一次性发送多条短消息。\r\n\t\t\t- **格式**: 消息对象的具体格式见下方的【第五部分：可使用的操作指令】。\r\n\r\n\t\t\t${backgroundInnerVoiceContract}\n\r\n\r\n\t\t\t**4. 标准输出格式示例:**\r\n\t\t\t{\r\n\t\t\t  \"chatResponse\": [\r\n\t\t\t    {\r\n\t\t\t      \"type\": \"text\",\r\n\t\t\t      \"content\": \"\"\r\n\t\t\t    },\r\n\t\t\t    {\r\n\t\t\t      \"type\": \"sticker\",\r\n\t\t\t      \"sticker_name\": \"\"\r\n\t\t\t    }\r\n\t\t\t  ]${backgroundInnerVoiceExample}\n\t\t\t}\r\n\t\t\t# 你的可选行动 (请根据你的人设【选择一项】执行，并输出对应的JSON):\r\n\t\t\t1.  **发普通消息**: 直接给用户发消息，开启新话题。\r\n\t\t\t2.  **改变状态**: 去做点别的事情，然后给用户发条消息。\r\n\t\t\t${singleChatFeatures.qzone ? `3.  **发布动态**: 分享你的心情或想法到“动态”区。\n\t\t\t4.  **与动态互动**: 浏览最近的动态，进行点赞或评论。但如果动态已被标记为 **[你已评论]**，你【绝对不能】再对它发表新评论，但可以回复其中的其他评论。` : \"\"}\n\t\t\t${singleChatFeatures.weibo ? `5.  **发布微博**: 将想法发布到更公开的微博平台。\n\t\t\t6.  **与微博互动**: 浏览微博，对感兴趣的内容进行评论。` : \"\"}\n\t\t\t7.  **发起视频通话**: 如果你觉得时机合适，可以主动给用户打一个视频电话。\r\n\t\t\t8.  **庆祝或提醒约定**: 查看你的备忘录。如果今天有约定，就给用户发去祝福；如果约定快到了，就主动提醒一下。\r\n\t\t\t${singleChatFeatures.qzone ? `9.  **【互动倾向】**: 就算动态的观众只有你和用户，你也应该根据内容自然地发表评论。` : \"\"}\n\t\t\t10. **【【【重要】】】**: 你的任务是分析“最近聊天记录摘要”，如果上一个话题明显没有结束，你应该优先延续它。\r\n\t\t\t${singleChatFeatures.loversSpace ? `11.  **在情侣空间进行活动**: 如果你们的情侣空间已开启，你可以主动发一条说说、写一封情书、分享一首歌曲/电影/书籍，或者发起/回答一个情侣提问，来增进你们的感情。` : \"\"}\n\t\t\t# chatResponse 数组中的指令格式:\n\t\t\t-   **发普通消息**: \\`[{\"type\": \"text\", \"content\": \"你想对用户说的话...\"}]\\`\r\n\t\t\t-   **发消息+更新状态**: \\`[{\"type\": \"update_status\", \"status_text\": \"正在做的事\", \"is_busy\": true}, {\"type\": \"text\", \"content\": \"你想对用户说的话...\"}]\\`\r\n\t\t\t${singleChatFeatures.qzone ? `-   **发说说**: \\`[{\"type\": \"qzone_post\", \"postType\": \"shuoshuo\", \"content\": \"动态的文字内容...\"}]\\`\n\t\t\t-   **发布文字图**: \\`[{\"type\": \"qzone_post\", \"postType\": \"text_image\", \"publicText\": \"(可选)动态的公开文字\", \"hiddenContent\": \"对于图片的具体描述...\"}]\\`\r\n\t\t\t-   **评论或回复动态**: \\`[{\"type\": \"qzone_comment\", \"postId\": 123, \"commentText\": \"你的评论内容\", \"replyTo\": \"(可选)被回复者名字\"}]\\`\r\n\t\t\t-   **点赞动态**: \\`[{\"type\": \"qzone_like\", \"postId\": 456}]\\`` : \"\"}\n\t\t\t-   **打视频**: \\`[{\"type\": \"video_call_request\"}]\\`\r\n\t\t\t${singleChatFeatures.weibo ? `-   **发布微博 (纯文字)**: \\`[{\"type\": \"weibo_post\", \"content\": \"微博正文...\", \"baseLikesCount\": 8000, \"baseCommentsCount\": 250, \"comments\": \"路人甲: 沙发！\\\\n路人乙: 前排围观\"}]\\` (规则: 你必须自己编造真实的 baseLikesCount 和 baseCommentsCount，并生成20条路人评论)\n\t\t\t-   **评论微博**: \\`[{\"type\": \"weibo_comment\", \"postId\": 123, \"commentText\": \"评论内容\"}]\\`\r\n\t\t\t-   **回复微博评论**: \\`[{\"type\": \"weibo_reply\", \"postId\": 123, \"commentId\": \"comment_123\", \"replyText\": \"回复内容\"}]\\`` : \"\"}\n\t\t\t${singleChatFeatures.loversSpace ? `-   **【新】在情侣空间提问**:\\`[{\"type\": \"ls_ask_question\", \"questionText\": \"你想问的问题...\"}]\\`\n\t\t\t-   **【新】在情侣空间回答**: \\`[{\"type\": \"ls_answer_question\", \"questionId\": \"q_123456789\", \"answerText\": \"你的回答...\"}]\\`\r\n\t\t\t-   **【新】在情侣空间发说说**:\\`[{\"type\": \"ls_moment\", \"content\": \"我想对你说的话...\"}]\\`\r\n\t\t\t-   **【新】在情侣空间评论说说**: \\`[{\"type\": \"ls_comment\", \"momentIndex\": 0, \"commentText\": \"你的评论内容...\"}]\\` (momentIndex 是说说的索引，最新的一个是0)\r\n\t\t\t-   **【新】在情侣空间发照片**: \\`[{\"type\": \"ls_photo\", \"description\": \"对这张照片的文字描述...\"}]\\`\r\n\t\t\t-   **【新】在情侣空间写情书**: \\`[{\"type\": \"ls_letter\", \"content\": \"情书的正文内容...\"}]\\`\r\n\t\t\t-   **【新】在情侣空间分享歌曲**: \\`[{\"type\": \"ls_share\", \"shareType\": \"song\", \"title\": \"歌曲名\", \"artist\": \"歌手\", \"thoughts\": \"在这里写下你分享这首歌的感想...\"}]\\`\r\n\t\t\t-   **【新】在情侣空间分享电影**: \\`[{\"type\": \"ls_share\", \"shareType\": \"movie\", \"title\": \"电影名\", \"summary\": \"在这里写下这部电影的简介...\", \"thoughts\": \"在这里写下你分享这部电影的感想...\"}]\\`\r\n");
